import styled from 'styled-components';

export const LinkWrapper = styled.div`

  ul {
    margin-left: 1.5rem;
    margin-bottom: 1rem;
  }
   
  h1, h2, h3 {
    font-size: 1.6rem;
    margin-top: 2rem;
    margin-bottom: .5rem;
    color: #eee;
    font-weight: 700;
    :before {
      position: absolute;
      left: 0;
      color: #eee;
      content: "#";
      font-weight: 700;
      font-size: 1.6rem;
    }
  }
  
`
